<%= form_for(@jqcalendar) do |f| %>
  <% if @jqcalendar.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@jqcalendar.errors.count, "error") %> prohibited this jqcalendar from being saved:</h2>

      <ul>
      <% @jqcalendar.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
